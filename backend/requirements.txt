fastapi==0.115.*
uvicorn[standard]==0.30.*
httpx==0.27.*
pydantic==2.*
python-dotenv==1.*
# --- auth (Stage 1): signed session cookies + OTP email via the Gmail API ---
# Password hashing uses stdlib hashlib.scrypt on purpose - no bcrypt/argon2 dependency.
# Gmail send uses httpx (already above) for the HTTP call; google-auth only mints the token.
itsdangerous==2.*
google-auth==2.*
python-docx==1.1.*     # ATS-safe DOCX resume/cover (electronic.py)
reportlab==4.2.*       # pure-Python PDF (no cairo/pango/wkhtmltopdf needed)
