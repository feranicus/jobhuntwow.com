# Never commit the candidate's live LinkedIn session or scraped output
linkedin_cookies.json
*.json
!package.json
.env
__pycache__/
credentials.json
events.jsonl
wd_*.json
recordings/
auth.json

# vision.dump() evidence: screenshot + DOM captured when every page reader goes blind.
# Diagnostic output (URLs, page contents), never source.
out/blind_*.png
out/blind_*.html
out/blind_*.json
